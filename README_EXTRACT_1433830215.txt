In the file MatchAudit_1433830215.txt, there is a tab-separated list of the
sources that contribute to each mastered Provider, along with a brief message 
explaining which fields were survived and which matching rule was used. The
file default_rules.yaml contains the mathcing rules and their names. 
